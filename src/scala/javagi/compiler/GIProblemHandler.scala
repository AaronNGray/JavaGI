package javagi.compiler

import javagi.eclipse.jdt.internal.compiler.ast._
import javagi.eclipse.jdt.internal.compiler.lookup._
import javagi.eclipse.jdt.internal.compiler.problem._

/*
object GIProblemHandler {
  def handleMissingType(mtb: MissingTypeBinding): Nothing = {
    val msg = "type \"" + mtb.debugName + "\" not found"
    System.err.println(msg)
    // mtb.environment.problemReporter.javaGIProblem(msg);
    throw new javagi.eclipse.jdt.internal.compiler.problem.AbortCompilation(msg)
  }
}
*/
